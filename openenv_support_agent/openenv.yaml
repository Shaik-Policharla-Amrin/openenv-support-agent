name: openenv-agent-final-1

metadata:
  name: openenv-agent-final-1
  description: Simple working OpenEnv agent

compute:
  cpu: 1

service:
  type: http
  port: 7860

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - id: task1
    input: "hello"
    expected_output: "Processed"

  - id: task2
    input: "Explain AI"
    expected_output: "Processed"

  - id: task3
    input: "What is OpenEnv"
    expected_output: "Processed"