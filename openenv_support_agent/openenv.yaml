name: openenv-support-agent

metadata:
  name: openenv-support-agent
  description: Simple AI support agent

compute:
  cpu: 1

service:
  type: http
  port: 7860

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - id: task1
    input: "hello"
    expected_output: "hello"

  - id: task2
    input: "Explain AI"
    expected_output: "AI"

  - id: task3
    input: "What is OpenEnv"
    expected_output: "OpenEnv"